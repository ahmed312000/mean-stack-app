import { Component } from "@angular/core";

@Component({
    selector : 'app-404',
    template : `
    <p class="lead text-danger">There is a problem wow</p>
    `
})

export class ErrorComponent {
    
}